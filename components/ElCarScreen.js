import { View,Text } from "react-native";
import styles from '../styles/styles';
const ElCarScreen = () => {
    return(
        <View style={styles.home}>
            
        </View>
    );
}
export {ElCarScreen}